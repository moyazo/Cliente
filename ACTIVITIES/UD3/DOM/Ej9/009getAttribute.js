"use strict"


const element = document.querySelector("div"); // Buscamos el elemento en cuestión

alert(element.dataset.widgetName);  // Cuando el atributo de la etiquet ano está definido en el DOM
                                    // Usamos dataset.
