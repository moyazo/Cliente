"use strict"

// elem.append(document.createTextNode("Hola soy un string"));  // Crea un nodo de texto    
// elem.innerHTML = "Hola soy un string";  // Crea un texto pero en HTML
// elem.textContent = "Hola soy un string";  // Crea simplemente un String

// document.createTextNode es el único que crea un nodo de texto por lo que crea por defecto un nodo.
// innerHTML y textContent hacen xactamente lo mismo