"use strict";

let a = prompt("Escribe un número");  // Pedimos al usuario un número
let b = prompt("Escribe otro número");

alert(a + b);// Muestra 55, ya que prompt devuelve un string. 

//si ponemos un + entre strings, estamos concatenado