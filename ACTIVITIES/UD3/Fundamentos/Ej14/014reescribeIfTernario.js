"use strict";

let result;
let a = 1;
let b = 3;
result = a + b < 4 ? "Baja la ropa sucia":"Sube los zapatos"; // si la condición es true, obtiene el primer valor si no el segundo
console.log(result);